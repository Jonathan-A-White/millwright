# The versions millwright's tools are pinned to. scripts/install.sh reads this
# file; the README and the join prompt point here rather than repeat the numbers.
# Assignments of a version only (make lint holds that), and GO_VERSION is the
# go line of go.mod (make lint holds that too).
GO_VERSION=1.27.1
BD_VERSION=1.3.0
